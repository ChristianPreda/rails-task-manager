<h1> My Tasks <h1>
  <h3>
  <% @tasks.each do |task| %>
    <% if task.completed%>
      [x]
    <% else %>
      [ ]
    <% end %>

    <%= link_to task.title, task_path(task) %>
    <br/>
  <% end %>
<br>
<%= link_to "create new task", new_task_path %>
</h3>

<!-- <h1>My tasks</h1>

<% @tasks.each do |task| %>
  <% if task.completed %>
  [x]
  <% else %>
  [ ]
  <% end %>


<%= link_to task.title, task_path(task) %>
<% end %>
 -->
